name: Build and Push Back Despachos Image

on:
  push:
    branches:
      - deploy

jobs:
  build-push:
    runs-on: ubuntu-latest

    steps:
      - name: Descargar codigo
        uses: actions/checkout@v4

      - name: Login Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_TOKEN }}

      - name: Build Back Despachos Image
        run: docker build -t ${{ secrets.DOCKER_USERNAME }}/back-despachos:latest .

      - name: Push Back Despachos Image
        run: docker push ${{ secrets.DOCKER_USERNAME }}/back-despachos:latest